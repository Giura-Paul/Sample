<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Sample.App.MainViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dto="using:Sample.DTO">

    <ItemsRepeater ItemsSource="{x:Bind PersonViewModel.Persons}">
        <DataTemplate x:DataType="dto:PersonDTO">
            <Grid>
                <TextBlock>
                    <Run Text="{x:Bind Id}"/>, <Run Text="{x:Bind LastName}"/>, <Run Text="{x:Bind FirstName}"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Edit" CommandParameter="{x:Bind}"/>
                    <Button Content="Delete"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </ItemsRepeater>

</UserControl>
